{
  "name": "electron-app",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "npx electron .",
    "package-win": "electron-packager . electron-app --platform=win32 --arch=x64 --out=dist --electron-version=16.0.5 --icon=icon.ico --overwrite"
  },
  "devDependencies": {
    "electron": "^31.2.1",
    "electron-packager": "^17.1.2"
  }
}
